# Vite only exposes env vars prefixed with VITE_

# 后端对接（开发推荐留空：请求同源 /api，由 vite 代理到 Next，默认 http://localhost:3000）
VITE_BACKEND_API_BASE=
VITE_IM_SOCKET_BASE=

# 仅开发：Vite 把 /api 代理到此地址（见 vite.config.ts）
VITE_BACKEND_PROXY_TARGET=http://localhost:3000
